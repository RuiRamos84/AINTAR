# /myapp/myapp/auth/services.py

def authenticate_user(username, password):
    # TODO: Implemente a lógica de autenticação aqui.
    # Este é apenas um exemplo de stub. No mundo real, você precisará
    # de uma maneira segura de verificar o usuário e a senha
    # contra um banco de dados de usuários ou similar.
    if username == "user" and password == "password":
        return True
    return False
